npx create-expo-app --template // to create an expo app
npx create-expo-app -t expo-template-blank-typescript
expo init my-project

npx expo start // to start the development server
expo --help // to check if the expo program is installed
cd react_native_1
- npm run android
- npm run ios # you need to use macOS to build the iOS project
                use the Expo app if you need to do iOS development without a Mac
- npm run web
